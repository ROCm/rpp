[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    hip: mark a test as requiring an AMD GPU (ROCm / HIP backend)
    slow: mark a test as slow (deselect with -m "not slow")
filterwarnings =
    error::DeprecationWarning
    error::RuntimeWarning
